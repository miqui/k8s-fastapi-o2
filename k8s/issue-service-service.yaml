apiVersion: v1
kind: Service
metadata:
  name: issue-service
  labels:
    app: issue-service
spec:
  type: ClusterIP
  selector:
    app: issue-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
