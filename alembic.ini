# Alembic config. The database URL is built from DB_* env vars in migrations/env.py (never here),
# and logging goes through app/logging.py (JSON on stdout), so there is no [loggers] section.
[alembic]
script_location = %(here)s/migrations
prepend_sys_path = .
